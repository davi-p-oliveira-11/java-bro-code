package VariableScope;

public class Main {

  // local = declared inside a method
  //         visible only to that method

  // global = declared outside a method, but withn a class
  //          visible to all parts of a class

  DiceRoller diceRoller = new DiceRoller();

}
